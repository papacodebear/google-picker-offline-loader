# google-picker-offline-loader
Load Google's Picker widget from vendored local files instead of live from apis.google.com — works in environments (like Firefox add-ons) that forbid remote script loading.
